# Concatenate all posts into a single text
all_posts_text <- paste(posts_text, collapse = " ")
# Count the words in the concatenated text
word_count <- count_words(all_posts_text)
return(word_count)
}
# URL of the phpBB thread
page_num_multipler <- as.character((page_num - 1) * 15)
thread_url <- paste0(
"https://www.youngwriterssociety.com/viewtopic.php?f=7&t=114683&start=", page_num_multipler)
# Get the word count
word_count <- scrape_phpbb_thread(thread_url)
# Print the word count
print(paste("Word count:", word_count))
library(rvest)
library(stringr)
# CHANGE THIS
page_num <- 16
# Function to count words in a string
count_words <- function(text) {
word_list <- unlist(str_split(text, "\\W"))
word_list <- word_list[word_list != ""]
return(length(word_list))
}
# Function to scrape and count words from a phpBB thread
scrape_phpbb_thread <- function(url) {
# Load the HTML content of the webpage
webpage <- read_html(url)
# Extract the text content of the posts and replace line breaks with spaces
posts_text <- webpage %>%
html_nodes(".post-text") %>%
html_text(trim = FALSE) %>%
sapply(function(x) gsub("\n", " ", x)) # Replace newline characters with spaces
# Concatenate all posts into a single text
all_posts_text <- paste(posts_text, collapse = " ")
# Count the words in the concatenated text
word_count <- count_words(all_posts_text)
return(word_count)
}
# URL of the phpBB thread
page_num_multipler <- as.character((page_num - 1) * 15)
thread_url <- paste0(
"https://www.youngwriterssociety.com/viewtopic.php?f=7&t=114683&start=", page_num_multipler)
# Get the word count
word_count <- scrape_phpbb_thread(thread_url)
# Print the word count
print(paste("Word count:", word_count))
library(rvest)
library(stringr)
# CHANGE THIS
page_num <- 17
# Function to count words in a string
count_words <- function(text) {
word_list <- unlist(str_split(text, "\\W"))
word_list <- word_list[word_list != ""]
return(length(word_list))
}
# Function to scrape and count words from a phpBB thread
scrape_phpbb_thread <- function(url) {
# Load the HTML content of the webpage
webpage <- read_html(url)
# Extract the text content of the posts and replace line breaks with spaces
posts_text <- webpage %>%
html_nodes(".post-text") %>%
html_text(trim = FALSE) %>%
sapply(function(x) gsub("\n", " ", x)) # Replace newline characters with spaces
# Concatenate all posts into a single text
all_posts_text <- paste(posts_text, collapse = " ")
# Count the words in the concatenated text
word_count <- count_words(all_posts_text)
return(word_count)
}
# URL of the phpBB thread
page_num_multipler <- as.character((page_num - 1) * 15)
thread_url <- paste0(
"https://www.youngwriterssociety.com/viewtopic.php?f=7&t=114683&start=", page_num_multipler)
# Get the word count
word_count <- scrape_phpbb_thread(thread_url)
# Print the word count
print(paste("Word count:", word_count))
library(rvest)
library(stringr)
# CHANGE THIS
page_num <- 18
# Function to count words in a string
count_words <- function(text) {
word_list <- unlist(str_split(text, "\\W"))
word_list <- word_list[word_list != ""]
return(length(word_list))
}
# Function to scrape and count words from a phpBB thread
scrape_phpbb_thread <- function(url) {
# Load the HTML content of the webpage
webpage <- read_html(url)
# Extract the text content of the posts and replace line breaks with spaces
posts_text <- webpage %>%
html_nodes(".post-text") %>%
html_text(trim = FALSE) %>%
sapply(function(x) gsub("\n", " ", x)) # Replace newline characters with spaces
# Concatenate all posts into a single text
all_posts_text <- paste(posts_text, collapse = " ")
# Count the words in the concatenated text
word_count <- count_words(all_posts_text)
return(word_count)
}
# URL of the phpBB thread
page_num_multipler <- as.character((page_num - 1) * 15)
thread_url <- paste0(
"https://www.youngwriterssociety.com/viewtopic.php?f=7&t=114683&start=", page_num_multipler)
# Get the word count
word_count <- scrape_phpbb_thread(thread_url)
# Print the word count
print(paste("Word count:", word_count))
library(rvest)
library(stringr)
# CHANGE THIS
page_num <- 19
# Function to count words in a string
count_words <- function(text) {
word_list <- unlist(str_split(text, "\\W"))
word_list <- word_list[word_list != ""]
return(length(word_list))
}
# Function to scrape and count words from a phpBB thread
scrape_phpbb_thread <- function(url) {
# Load the HTML content of the webpage
webpage <- read_html(url)
# Extract the text content of the posts and replace line breaks with spaces
posts_text <- webpage %>%
html_nodes(".post-text") %>%
html_text(trim = FALSE) %>%
sapply(function(x) gsub("\n", " ", x)) # Replace newline characters with spaces
# Concatenate all posts into a single text
all_posts_text <- paste(posts_text, collapse = " ")
# Count the words in the concatenated text
word_count <- count_words(all_posts_text)
return(word_count)
}
# URL of the phpBB thread
page_num_multipler <- as.character((page_num - 1) * 15)
thread_url <- paste0(
"https://www.youngwriterssociety.com/viewtopic.php?f=7&t=114683&start=", page_num_multipler)
# Get the word count
word_count <- scrape_phpbb_thread(thread_url)
# Print the word count
print(paste("Word count:", word_count))
library(rvest)
library(stringr)
# CHANGE THIS
page_num <- 20
# Function to count words in a string
count_words <- function(text) {
word_list <- unlist(str_split(text, "\\W"))
word_list <- word_list[word_list != ""]
return(length(word_list))
}
# Function to scrape and count words from a phpBB thread
scrape_phpbb_thread <- function(url) {
# Load the HTML content of the webpage
webpage <- read_html(url)
# Extract the text content of the posts and replace line breaks with spaces
posts_text <- webpage %>%
html_nodes(".post-text") %>%
html_text(trim = FALSE) %>%
sapply(function(x) gsub("\n", " ", x)) # Replace newline characters with spaces
# Concatenate all posts into a single text
all_posts_text <- paste(posts_text, collapse = " ")
# Count the words in the concatenated text
word_count <- count_words(all_posts_text)
return(word_count)
}
# URL of the phpBB thread
page_num_multipler <- as.character((page_num - 1) * 15)
thread_url <- paste0(
"https://www.youngwriterssociety.com/viewtopic.php?f=7&t=114683&start=", page_num_multipler)
# Get the word count
word_count <- scrape_phpbb_thread(thread_url)
# Print the word count
print(paste("Word count:", word_count))
library(rvest)
library(stringr)
library(purrr) # Ensure this is installed or use library(tidyverse)
# INPUTS
# Base URL of the phpBB thread without the 'start' parameter
base_url <- "https://www.youngwriterssociety.com/viewtopic.php?f=191&t=111952"
# Parameters for the loop
start_page <- 0 # Start from the first page
end_page <- 1470 # End at the last page (this value might need to be adjusted)
page_step <- 15 # Assuming each page has 15 posts (adjust as needed)
# -----------------------------------
# Function to count words in a string
count_words <- function(text) {
word_list <- unlist(str_split(text, "\\W"))
word_list <- word_list[word_list != ""]
return(length(word_list))
}
# Function to scrape and count words from a phpBB thread page
scrape_phpbb_thread_page <- function(url) {
# Load the HTML content of the webpage
webpage <- read_html(url)
# Extract the text content of the posts
posts_text <- webpage %>%
html_nodes(".post-text") %>%
map_chr(~html_text(.x, trim = TRUE)) %>%
# Replace line breaks with a space to ensure word boundaries are preserved
str_replace_all("\n", " ")
# Concatenate all posts into a single text with spaces between them
all_posts_text <- paste(posts_text, collapse = " ")
# Count the words in the concatenated text
word_count <- count_words(all_posts_text)
return(word_count)
}
# Function to iterate over pages and sum word counts
scrape_phpbb_thread <- function(base_url, start, end, step) {
total_word_count <- 0
for (i in seq(start, end, by = step)) {
# Construct the URL for each page
page_url <- paste0(base_url, "&start=", i)
# Sum the word count for each page
total_word_count <- total_word_count + scrape_phpbb_thread_page(page_url)
}
return(total_word_count)
}
# Get the total word count for all pages
total_word_count <- scrape_phpbb_thread(base_url, start_page, end_page, page_step)
# Print the total word count
print(paste("Total word count:", total_word_count))
library(rvest)
library(stringr)
library(purrr) # Ensure this is installed or use library(tidyverse)
# INPUTS
# Base URL of the phpBB thread without the 'start' parameter
base_url <- "https://www.youngwriterssociety.com/viewtopic.php?f=191&t=112182"
# Parameters for the loop
start_page <- 0 # Start from the first page
end_page <- 795 # End at the last page (this value might need to be adjusted)
page_step <- 15 # Assuming each page has 15 posts (adjust as needed)
# -----------------------------------
# Function to count words in a string
count_words <- function(text) {
word_list <- unlist(str_split(text, "\\W"))
word_list <- word_list[word_list != ""]
return(length(word_list))
}
# Function to scrape and count words from a phpBB thread page
scrape_phpbb_thread_page <- function(url) {
# Load the HTML content of the webpage
webpage <- read_html(url)
# Extract the text content of the posts
posts_text <- webpage %>%
html_nodes(".post-text") %>%
map_chr(~html_text(.x, trim = TRUE)) %>%
# Replace line breaks with a space to ensure word boundaries are preserved
str_replace_all("\n", " ")
# Concatenate all posts into a single text with spaces between them
all_posts_text <- paste(posts_text, collapse = " ")
# Count the words in the concatenated text
word_count <- count_words(all_posts_text)
return(word_count)
}
# Function to iterate over pages and sum word counts
scrape_phpbb_thread <- function(base_url, start, end, step) {
total_word_count <- 0
for (i in seq(start, end, by = step)) {
# Construct the URL for each page
page_url <- paste0(base_url, "&start=", i)
# Sum the word count for each page
total_word_count <- total_word_count + scrape_phpbb_thread_page(page_url)
}
return(total_word_count)
}
# Get the total word count for all pages
total_word_count <- scrape_phpbb_thread(base_url, start_page, end_page, page_step)
# Print the total word count
print(paste("Total word count:", total_word_count))
library(rvest)
library(stringr)
library(purrr) # Ensure this is installed or use library(tidyverse)
# INPUTS
# Base URL of the phpBB thread without the 'start' parameter
base_url <- "https://www.youngwriterssociety.com/viewtopic.php?f=461&t=112522"
# Parameters for the loop
start_page <- 0 # Start from the first page
end_page <- 660 # End at the last page (this value might need to be adjusted)
page_step <- 15 # Assuming each page has 15 posts (adjust as needed)
# -----------------------------------
# Function to count words in a string
count_words <- function(text) {
word_list <- unlist(str_split(text, "\\W"))
word_list <- word_list[word_list != ""]
return(length(word_list))
}
# Function to scrape and count words from a phpBB thread page
scrape_phpbb_thread_page <- function(url) {
# Load the HTML content of the webpage
webpage <- read_html(url)
# Extract the text content of the posts
posts_text <- webpage %>%
html_nodes(".post-text") %>%
map_chr(~html_text(.x, trim = TRUE)) %>%
# Replace line breaks with a space to ensure word boundaries are preserved
str_replace_all("\n", " ")
# Concatenate all posts into a single text with spaces between them
all_posts_text <- paste(posts_text, collapse = " ")
# Count the words in the concatenated text
word_count <- count_words(all_posts_text)
return(word_count)
}
# Function to iterate over pages and sum word counts
scrape_phpbb_thread <- function(base_url, start, end, step) {
total_word_count <- 0
for (i in seq(start, end, by = step)) {
# Construct the URL for each page
page_url <- paste0(base_url, "&start=", i)
# Sum the word count for each page
total_word_count <- total_word_count + scrape_phpbb_thread_page(page_url)
}
return(total_word_count)
}
# Get the total word count for all pages
total_word_count <- scrape_phpbb_thread(base_url, start_page, end_page, page_step)
# Print the total word count
print(paste("Total word count:", total_word_count))
library(rvest)
library(stringr)
library(purrr) # Ensure this is installed or use library(tidyverse)
# INPUTS
# Base URL of the phpBB thread without the 'start' parameter
base_url <- "https://www.youngwriterssociety.com/viewtopic.php?f=191&t=113018"
# Parameters for the loop
start_page <- 0 # Start from the first page
end_page <- 195 # End at the last page (this value might need to be adjusted)
page_step <- 15 # Assuming each page has 15 posts (adjust as needed)
# -----------------------------------
# Function to count words in a string
count_words <- function(text) {
word_list <- unlist(str_split(text, "\\W"))
word_list <- word_list[word_list != ""]
return(length(word_list))
}
# Function to scrape and count words from a phpBB thread page
scrape_phpbb_thread_page <- function(url) {
# Load the HTML content of the webpage
webpage <- read_html(url)
# Extract the text content of the posts
posts_text <- webpage %>%
html_nodes(".post-text") %>%
map_chr(~html_text(.x, trim = TRUE)) %>%
# Replace line breaks with a space to ensure word boundaries are preserved
str_replace_all("\n", " ")
# Concatenate all posts into a single text with spaces between them
all_posts_text <- paste(posts_text, collapse = " ")
# Count the words in the concatenated text
word_count <- count_words(all_posts_text)
return(word_count)
}
# Function to iterate over pages and sum word counts
scrape_phpbb_thread <- function(base_url, start, end, step) {
total_word_count <- 0
for (i in seq(start, end, by = step)) {
# Construct the URL for each page
page_url <- paste0(base_url, "&start=", i)
# Sum the word count for each page
total_word_count <- total_word_count + scrape_phpbb_thread_page(page_url)
}
return(total_word_count)
}
# Get the total word count for all pages
total_word_count <- scrape_phpbb_thread(base_url, start_page, end_page, page_step)
# Print the total word count
print(paste("Total word count:", total_word_count))
paste0(
"https://www.youngwriterssociety.com/viewtopic.php?f=7&t=114683&start=",
page_num_multipler
)
page_num_multipler <- 20
paste0(
"https://www.youngwriterssociety.com/viewtopic.php?f=7&t=114683&start=",
page_num_multipler
)
paste0(
"https://www.youngwriterssociety.com/viewtopic.php?f=7&t=114683&start=",
page_num_multipler, "hi"
)
paste0(
"https://www.youngwriterssociety.com/viewtopic.php?f=7&t=114683&start=",
"&start=",
page_num_multipler)
paste0(
"https://www.youngwriterssociety.com/viewtopic.php?f=7&t=114683",
"&start=",
page_num_multipler)
library(rvest)
library(stringr)
# INPUTS
# input the page number you want to count
page_num <- 20
# URL of  phpBB thread - first page only
first_page_url <- "https://www.youngwriterssociety.com/viewtopic.php?f=7&t=114683"
# -----------------------------------
# function to simplify getting thread URL
get_thread_url <- function(url) {
thread_url <- paste0(
link,
"&start=",
as.character((page_num - 1) * 15))
}
# function to count words in a string
count_words <- function(text) {
word_list <- unlist(str_split(text, "\\W"))
word_list <- word_list[word_list != ""]
return(length(word_list))
}
# function to scrape and count words from a phpBB thread
scrape_phpbb_thread <- function(url) {
# load the HTML content of page
webpage <- read_html(url)
# extract the text content of the posts
posts_text <- webpage %>%
html_nodes(".post-text") %>%
html_text(trim = FALSE) %>%
# replace newline characters with spaces
sapply(function(x) gsub("\n", " ", x))
# concatenate all posts into words
all_posts_text <- paste(posts_text, collapse = " ")
# count the words
word_count <- count_words(all_posts_text)
return(word_count)
}
# get URL
thread_url <- get_thread_url
# Get the word count
word_count <- scrape_phpbb_thread(thread_url)
library(rvest)
library(stringr)
# input the page number you want to count
page_num <- 20
first_page_url <- "https://www.youngwriterssociety.com/viewtopic.php?f=7&t=114683"
# function to simplify getting thread URL
get_thread_url <- function(url) {
thread_url <- paste0(
link,
"&start=",
as.character((page_num - 1) * 15))
}
# function to count words in a string
count_words <- function(text) {
word_list <- unlist(str_split(text, "\\W"))
word_list <- word_list[word_list != ""]
return(length(word_list))
}
# function to scrape and count words from a phpBB thread
scrape_phpbb_thread <- function(url) {
# load the HTML content of page
webpage <- read_html(url)
# extract the text content of the posts
posts_text <- webpage %>%
html_nodes(".post-text") %>%
html_text(trim = FALSE) %>%
# replace newline characters with spaces
sapply(function(x) gsub("\n", " ", x))
# concatenate all posts into words
all_posts_text <- paste(posts_text, collapse = " ")
# count the words
word_count <- count_words(all_posts_text)
return(word_count)
}
# get URL
thread_url <- get_thread_url
# Get the word count
word_count <- scrape_phpbb_thread(thread_url)
# get URL
thread_url <- get_thread_url
# get URL
thread_url <- get_thread_url(first_page_url)
# function to simplify getting thread URL
get_thread_url <- function(url) {
thread_url <- paste0(
url,
"&start=",
as.character((page_num - 1) * 15))
}
# function to count words in a string
count_words <- function(text) {
word_list <- unlist(str_split(text, "\\W"))
word_list <- word_list[word_list != ""]
return(length(word_list))
}
# function to scrape and count words from a phpBB thread
scrape_phpbb_thread <- function(url) {
# load the HTML content of page
webpage <- read_html(url)
# extract the text content of the posts
posts_text <- webpage %>%
html_nodes(".post-text") %>%
html_text(trim = FALSE) %>%
# replace newline characters with spaces
sapply(function(x) gsub("\n", " ", x))
# concatenate all posts into words
all_posts_text <- paste(posts_text, collapse = " ")
# count the words
word_count <- count_words(all_posts_text)
return(word_count)
}
# get URL
thread_url <- get_thread_url(first_page_url)
# Get the word count
word_count <- scrape_phpbb_thread(thread_url)
# Print the word count
print(paste("Word count:", word_count))
renv::status()
renv::snapshot()
